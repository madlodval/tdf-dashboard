{
  "name": "@tdf/database",
  "version": "1.0.0",
  "description": "Database handlers for TradingDifferent project (supports MySQL and PostgreSQL)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "eslint . --ext .js,.mjs",
    "format": "prettier --write .",
    "build": "echo \"No build step required\""
  },
  "keywords": ["database", "mysql", "postgresql"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^3.9.7",
    "dotenv": "^16.4.5"
  }
}
